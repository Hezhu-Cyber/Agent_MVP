# Agent_MVP
A minimal Agent Runtime built from scratch without Agent frameworks, featuring LLM tool calling, session isolation, persistent memory, context compression, execution tracing, and a local web UI.
