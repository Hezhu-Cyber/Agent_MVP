**模块一：Context / Performance**

**一个 session 连续聊了 200 轮，context 快爆了。你会怎么做压缩？如何确保压缩后的对话仍然流畅？**

**答：**

我不会直接把 200 轮对话总结成短文，而是把每轮对话转换成“状态变化”。

具体分四步：

第一，抽取。每轮只提取会影响后续行动的信息，例如目标变化、新增约束、关键决定、失败原因和未完成事项。

第二，合并。相同内容只保留一份；新信息覆盖旧信息，但保留变化原因。例如用户先选择 A、后来改成 B，压缩后记录“当前采用 B，A 因成本过高被放弃”，而不是保留两段重复讨论。

第三，淘汰。删除寒暄、重复解释、已完成的中间过程和大段工具日志，只保留其结论及原始记录位置。

第四，保留。最近若干轮不压缩，用来理解“刚才那个方案”等指代；远期内容则压缩成当前状态，并在需要时从原始记录中检索。

因此，每次压缩后的上下文由四部分组成：“当前目标与约束 + 关键决定及原因 + 未完成事项 + 最近几轮原文”。

压缩的本质，是把按时间展开的对话历史，折叠成面向下一步行动的状态，而不是单纯缩短文字。



**模块二：Memory**

**和聊天 Agent 熟悉半个月后，用户问了一个以前问过的问题。Agent 如何做 memory 召回更合理？**



用户再次提出以前问过的问题时，Agent 不应该只搜索最相似的历史消息，然后照搬旧答案。因为问题虽然相同，用户当前的状态、目的和外部事实可能已经变化。

我会把召回分为四步。

第一，识别问题。判断这是完全重复的问题、基于旧问题的追问，还是用户在新场景下重新提问。

第二，召回完整事件。除了找回以前的问题和回答，还要召回用户当时的反馈、做出的决定、否定过的方案，以及半个月内相关信息的变化。

第三，检查有效性。按照“用户最新明确表达优先、当前状态优先、可靠来源优先”的原则，过滤已经过期或被纠正的记忆。如果新旧信息冲突，应使用最新信息，同时保留变化原因。

第四，重新生成回答。Agent 应结合当前问题重新推理，而不是直接复读旧答案。例如可以说：“你之前也关注过这个问题，当时更看重成本；如果现在仍以成本为主，原来的方案依然合适。但结合你最近新增的实时性要求，我会调整为……”

因此，合理的记忆召回结果应该包含：“以前的问题与回答 + 用户当时的反馈 + 后续状态变化 + 当前提问场景”。

召回质量也不应只看是否找到了相似文本，而应看 Agent 是否避免重复解释、错误使用过期信息，并能基于用户最新状态给出更合适的答案。

本质上，Memory 召回不是寻找“过去说过什么”，而是恢复“当时为什么这样回答”，再判断这个答案今天是否仍然成立。



**模块三：Task** 

**对于长程任务，大模型执行一段时间可能会忘掉目标，你知道哪些解决方案，有什么优缺？**



长程任务中，大量中间信息会逐渐稀释最初目标，模型也可能把当前子任务误当成最终目标。常见解决方案有四类。

第一，固定注入目标。每轮都向模型提供总目标和硬性约束。优点是简单可靠，缺点是会持续占用上下文，而且目标太长时仍可能被忽略。

第二，维护结构化任务状态。将任务整理成“总目标、当前阶段、已完成事项、下一步和关键约束”。优点是进度清楚、便于恢复；缺点是状态需要及时更新，一旦记录错误，后续执行也会被带偏。

第三，采用分层规划和检查点。把任务拆成“总目标—阶段目标—当前动作”，每完成一个阶段就保存结果和剩余任务。优点是能降低长任务的复杂度，并支持回滚；缺点是初始计划可能不准确，过于固定的计划也会限制灵活性。

第四，定期进行目标校验。每执行若干步，就检查“当前行动是否仍然服务于总目标”，必要时也可以由独立的监督 Agent 负责检查。优点是能够及时发现目标漂移；缺点是会增加调用成本，而且模型不一定能发现自己的错误。

实际设计中，我会组合使用这些方法：固定保存总目标和硬约束，维护结构化任务状态，阶段结束时生成检查点，并定期检查当前行动与总目标是否一致。发现偏离后，回到最近的检查点重新规划。

评价效果时，也不能只看模型能否复述目标，而要看它是否减少了无关行动、能否及时发现偏离，以及偏离后能否快速恢复。

本质上，解决长程任务中的目标遗忘，不是让模型拥有更强的记忆，而是让目标始终可见、进度始终可检查、错误始终可恢复。



**模块四：Tool / Session Runtime**

**Agent 工具有同步和异步两类。异步工具不能让用户一直等，但结果依然重要。你会如何设计异步工具执行和完成通知？**



对于异步工具，我会将“提交任务”和“获取结果”拆成两个阶段，避免用户一直停留在当前页面等待。

第一，提交任务。Agent 调用异步工具后，系统立即返回任务编号、当前状态和预计处理方式，并告诉用户任务已经开始，可以继续进行其他操作。任务请求会被写入持久化队列，避免服务重启后丢失。

第二，后台执行。工作节点从队列中领取任务，并通过“等待中、执行中、成功、失败、超时、已取消”等状态管理整个生命周期。任务需要支持幂等、超时、重试和取消，防止重复执行或无限等待。

第三，保存结果。工具完成后，应先把完整结果写入结果存储，再发送完成事件。这样即使通知失败，结果仍然存在，用户之后也可以通过任务编号查询。

第四，完成通知。系统根据用户当前状态选择通知方式：如果用户仍在当前会话中，就在会话里更新任务状态；如果用户已经离开，则发送应用内通知。通知只提供结果摘要、成功或失败状态以及下一步操作，完整结果按需展开。

结果返回时，Agent 还需要判断它是否仍然有效。例如用户已经修改了任务参数，旧任务的结果就不能直接写入当前状态，而应标记为“基于旧版本生成”，由用户决定是否采用。

整个流程可以表示为：“提交任务 → 返回任务编号 → 后台执行 → 保存结果 → 发送完成事件 → 更新会话或通知用户”。

系统还应处理通知重复、任务重试和服务重启等情况。每个任务和通知都带有唯一编号，确保任务最多产生一份有效结果，重复通知也不会被用户看到多次。

本质上，异步工具设计的重点不是“让任务在后台运行”，而是保证用户无需等待，同时任务有状态、结果不丢失、通知不重复，过期结果也不会错误地影响当前对话。



**模块五：Agent Runtime 架构对比**

**Claude Code 的工具输出方式和国内 GLM / 豆包等 OpenAI-compatible function calling 有什么不同？他们各自这样设计的优缺点是什么？**

严格来说，Claude Code 是完整的 Agent 产品，而 GLM、豆包等提供的 OpenAI-compatible function calling 是一种 API 协议。如果只比较工具调用和结果返回方式，两者主要有以下区别。

第一，Claude 采用“内容块”设计。模型通过 `tool\_use` 内容块发起调用，工具执行结果再通过 `tool\_result` 内容块返回。文字、工具调用和工具结果都属于对话内容的一部分。

这种设计表达能力较强，可以自然地组织文字、多个工具调用和不同类型的结果，适合 Claude Code 这类复杂 Agent。缺点是消息结构较复杂，对内容顺序要求严格，接入和适配成本较高。

第二，OpenAI-compatible 采用“函数调用”设计。模型通过 `tool\_calls` 返回函数名和参数，程序执行工具后，再使用独立的 `role: tool` 消息返回结果，并通过 `tool\_call\_id` 与调用对应。

这种设计格式简单、生态成熟，GLM、豆包等模型可以较容易地接入现有框架。缺点是复杂结果通常需要自行序列化，而且不同厂商在并行调用、流式输出和参数支持等方面不一定完全兼容。

总体而言，Claude 的设计更重视完整表达 Agent 的执行过程，适合复杂工具链；OpenAI-compatible 更重视接口统一和生态兼容，适合多模型接入。实际开发中，可以先定义统一的内部工具格式，再通过适配器转换成不同厂商的协议，降低系统对单一模型的依赖。





